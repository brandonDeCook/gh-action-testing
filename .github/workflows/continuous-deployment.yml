name: Continuous Deployment

on:
  workflow_call:

permissions:
  id-token: write
  contents: read

jobs:
  display-variables:
    runs-on: ubuntu-latest
    steps:
      - name: Variables
        run: |
          echo "GITHUB_REF: $GITHUB_REF"

